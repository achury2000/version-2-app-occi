import 'package:flutter/material.dart';

/// DEPRECATED: Este archivo contiene providers de ejemplo antiguos.
/// 
/// Usa en su lugar:
/// - [AuthProvider] para autenticación y usuario
/// - [CatalogoProvider] para fincas y rutas
/// 
/// TODO: Eliminar este archivo cuando todo esté migrado a los providers reales.

/*
// Clases de ejemplo comentadas porque ya no se usan
// Usa AuthProvider de auth_provider.dart en su lugar

class TourProvider extends ChangeNotifier {
  bool _isLoading = false;
  bool get isLoading => _isLoading;
}

class UserProvider extends ChangeNotifier {
  bool _isLoading = false;
  bool get isLoading => _isLoading;
}
*/
